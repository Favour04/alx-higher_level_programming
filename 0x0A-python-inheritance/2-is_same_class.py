#!/usr/bin/python3
"""
Moule contain the
function is_same_class
which check if an obj
is from the class a_class
"""


def is_same_class(obj, a_class):
    """
    this function as defined
    in the doc earlier returns
    True if obj is from the s-
    ame class as a_class
    """
    if type(obj) is a_class:
        return True
