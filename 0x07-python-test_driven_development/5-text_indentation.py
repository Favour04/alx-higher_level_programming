#!/usr/bin/python3

def text_indentation(text):
    for each_charcter in text:
        if each_character = '?' or each_character = '.':
            print('{}\n'.format(each_character))
        else:
            print('{}'.format(each_character) + '')

